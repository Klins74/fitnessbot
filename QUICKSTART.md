# 🚀 Быстрый старт

## Шаг 1: Получить токен бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям (придумайте имя и username бота)
5. Скопируйте полученный токен

## Шаг 2: Настроить .env файл

Скопируйте `.env.example` в `.env` (или создайте новый файл `.env`):

```bash
copy .env.example .env
```

Откройте `.env` и вставьте ваш токен:

```env
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
DATABASE_URL=sqlite+aiosqlite:///fitness_bot.db
```

## Шаг 3: Установить зависимости

```bash
# Создать виртуальное окружение (опционально, но рекомендуется)
python -m venv venv
venv\Scripts\activate

# Установить пакеты
pip install -r requirements.txt
```

## Шаг 4: Загрузить тренировки

```bash
python load_workouts.py
```

Вы должны увидеть:
```
✅ Загружена тренировка: home_beginner_lose_weight_mon
✅ Загружена тренировка: home_beginner_lose_weight_wed
...
✅ Все тренировки загружены!
```

## Шаг 5: Запустить бота

```bash
python main.py
```

Вы должны увидеть:
```
Инициализация базы данных...
База данных готова
Бот запускается...
```

## Шаг 6: Тестирование

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте `/start`
4. Пройдите онбординг
5. Попробуйте все функции:
   - 🏋️ Бүгінгі жаттығу
   - 📊 Менің нәтижелерім
   - ⚙️ Жоспарды өзгерту
   - 🔔 Еске салғыштар

## 🐛 Возможные проблемы

### Проблема: `BOT_TOKEN не установлен`
**Решение:** Проверьте, что файл `.env` существует и содержит правильный токен.

### Проблема: `ModuleNotFoundError: No module named 'aiogram'`
**Решение:** Установите зависимости: `pip install -r requirements.txt`

### Проблема: База данных не создается
**Решение:** Убедитесь, что у вас есть права на запись в текущую директорию.

## 📝 Что дальше?

1. **Добавить больше тренировок** - отредактируйте `load_workouts.py`
2. **Настроить напоминания** - APScheduler уже интегрирован
3. **Деплоить на сервер** - используйте Dockerfile или Render.com
4. **Добавить аналитику** - интегрируйте Ollama API для AI-советов

## 💡 Советы

- Регулярно делайте бэкап `fitness_bot.db`
- Используйте `git` для версионирования
- Добавьте мониторинг в production (например, Sentry)
- Тестируйте изменения в отдельном test-боте

## 🆘 Помощь

Если возникли проблемы:
1. Проверьте логи в `bot.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте версию Python (должна быть 3.11+)

---

**Удачи! 🎉**
