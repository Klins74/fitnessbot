# 🏋️ Фитнес-бот на казахском языке

Telegram-бот личный фитнес-тренер на казахском языке, который помогает пользователям:

- 📋 Создать персональный профиль
- 💪 Получать персональные планы тренировок
- ✅ Отмечать выполненные тренировки
- 📊 Отслеживать прогресс
- 🔔 Настраивать напоминания

## 🚀 Технологии

- **Python 3.11+**
- **aiogram 3.x** - современный фреймворк для Telegram ботов
- **SQLAlchemy 2.x** - ORM для работы с БД
- **SQLite** - база данных (легко мигрирует на PostgreSQL)
- **APScheduler** - планировщик для напоминаний

## 📁 Структура проекта

```
fitness-bot-kz/
├── main.py                 # Точка входа
├── config.py               # Конфигурация
├── texts_kk.py             # Тексты на казахском
├── keyboards.py            # Клавиатуры бота
├── load_workouts.py        # Скрипт загрузки тренировок
├── handlers/               # Обработчики команд
│   ├── onboarding.py       # Онбординг (FSM)
│   ├── menu.py             # Главное меню
│   ├── workouts.py         # Тренировки и прогресс
│   └── states.py           # FSM состояния
├── services/               # Бизнес-логика
│   ├── users.py            # Работа с пользователями
│   └── workouts.py         # Работа с тренировками
├── db/                     # База данных
│   ├── models.py           # Модели SQLAlchemy
│   └── session.py          # Управление сессиями
├── utils/                  # Утилиты
│   ├── validators.py       # Валидация ввода
│   └── formatters.py       # Форматирование данных
├── requirements.txt        # Зависимости
├── .env.example            # Пример переменных окружения
└── README.md               # Документация
```

## ⚙️ Установка и запуск

### 1. Клонировать репозиторий

```bash
cd fitness-bot-kz
```

### 2. Создать виртуальное окружение

```bash
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

### 4. Настроить переменные окружения

Скопируйте `.env.example` в `.env` и заполните:

```env
BOT_TOKEN=your_telegram_bot_token_here
DATABASE_URL=sqlite+aiosqlite:///fitness_bot.db
```

Получить токен бота можно у [@BotFather](https://t.me/BotFather) в Telegram.

### 5. Загрузить начальные тренировки

```bash
python load_workouts.py
```

### 6. Запустить бота

```bash
python main.py
```

## 🎯 Основные функции

### Онбординг
- Сбор профиля пользователя (пол, возраст, рост, вес)
- Выбор цели (похудение, набор массы, поддержание формы)
- Выбор уровня (начинающий, средний)
- Выбор места тренировок (дом, зал)

### Тренировки
- Автоматический подбор тренировки на сегодня
- Список упражнений с подходами и повторениями
- Отметка выполнения с оценкой сложности

### Прогресс
- Общее количество тренировок
- Статистика за 7 и 30 дней
- Средняя оценка сложности

### Напоминания
- Настройка времени напоминаний
- Ежедневные уведомления о тренировке

## 🗃️ База данных

### Модели:

**User** - пользователи
- Профиль (возраст, рост, вес, цель, уровень)
- Настройки напоминаний

**Workout** - шаблоны тренировок
- Параметры (уровень, тип, цель, день недели)
- Упражнения (JSON)

**UserWorkout** - выполненные тренировки
- Дата выполнения
- Оценка сложности
- Комментарий

## 🐳 Docker (опционально)

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["python", "main.py"]
```

```bash
docker build -t fitness-bot .
docker run -d --env-file .env fitness-bot
```

## 🚀 Деплой

### Вариант 1: Render.com / Railway

1. Загрузите код на GitHub
2. Подключите репозиторий к Render/Railway
3. Добавьте переменные окружения
4. Деплой!

### Вариант 2: VPS

```bash
# Установить зависимости
pip install -r requirements.txt

# Запустить через systemd или screen
python main.py
```

## 📝 Добавление новых тренировок

Отредактируйте `load_workouts.py` и добавьте новый элемент в `WORKOUT_TEMPLATES`:

```python
{
    "code": "home_beginner_lose_weight_sun",  # уникальный код
    "title": "Воскресная тренировка",
    "level": "beginner",                      # beginner | intermediate
    "workout_type": "home",                   # home | gym
    "goal": "lose_weight",                    # lose_weight | gain_muscle | stay_fit
    "day_index": 6,                           # 0-6 (пн-вс)
    "exercises": [
        {"name": "Упражнение", "sets": 3, "reps": "10-15"}
    ]
}
```

Затем запустите: `python load_workouts.py`

## 🔧 Настройка валидации

В `config.py` можно изменить диапазоны валидации:

```python
MIN_AGE = 10
MAX_AGE = 90
MIN_HEIGHT = 120  # см
MAX_HEIGHT = 250  # см
MIN_WEIGHT = 30   # кг
MAX_WEIGHT = 300  # кг
```

## 🌐 Локализация

Все тексты находятся в `texts_kk.py`. Чтобы добавить другой язык:

1. Создайте `texts_ru.py` или `texts_en.py`
2. Скопируйте структуру из `texts_kk.py`
3. Переведите тексты
4. Обновите импорты в handlers

## 🐛 Логирование

Логи сохраняются в:
- Консоль (stdout)
- Файл `bot.log`

## 📄 Лицензия

MIT License

## 🤝 Контакты

По вопросам и предложениям: [Telegram]

---

**Сделано с ❤️ для казахстанского фитнес-сообщества** 🇰🇿
